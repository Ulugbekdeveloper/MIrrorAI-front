# Copy to .env and fill in.
# EXPO_PUBLIC_* vars are inlined into the JS bundle — safe for public URLs,
# NEVER for secrets like the Replicate API token.

# Your NestJS gateway base URL.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# Optional: request timeout in ms.
EXPO_PUBLIC_API_TIMEOUT_MS=30000
